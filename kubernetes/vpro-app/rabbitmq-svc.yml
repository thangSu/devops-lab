apiVersion: v1
kind: Service
metadata:
  name: dev-rabbitmq-svc
  namespace: example-app
spec:
  ports:
  - port: 5672
    targetPort: dev-rb-port
    protocol: TCP
  selector:
    app: dev-rb-svc
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: dev-rabbitmq-svc
  namespace: example-app
spec:
  ports:
  - port: 5672
    targetPort: dev-rabbitmq-port
    protocol: TCP
  selector:
    app: dev-rabbitmq-svc
  type: ClusterIP
